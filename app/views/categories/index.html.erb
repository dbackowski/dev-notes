<% unless @categories.blank? %>
  <ul>
    <% @categories.each do |category| %>
      <li><%= link_to category.name, category_path(category) %>&nbsp;(<%= category.notes.count %>)</li>
    <% end %>
  </ul>
<% else %>
  <%= info 'No categories found.' %>
<% end %>